export const setProvider = (provider) => ({
    type: 'SET_PROVIDER',
    provider
});

export const setShift = (shift) => ({
    type: 'SET_SHIFT',
    shift
});

export const setOpeningSession = (openingSession) => ({
    type: 'SET_OPENING_SESSION',
    openingSession
});

export const setProviderStatus = (providerStatus) => ({
    type: 'SET_PROVIDER_STATUS',
    providerStatus
});